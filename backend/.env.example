# Broker MQTT (TLS) — valeurs du groupe, à recopier dans un .env local
MQTT_URL=mqtts://<host>:8883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_GROUP=groupeX

# Serveur HTTP + WebSocket
PORT=3000

# Base SQLite
DB_PATH=./data.db
