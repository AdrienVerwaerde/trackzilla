# Copy to .env and fill in. Never commit .env.

# Broker (TLS), group credentials from day 2
MQTT_URL=mqtts://broker.example.com:8883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_GROUP=groupeX

# HTTP + WebSocket
PORT=3000
